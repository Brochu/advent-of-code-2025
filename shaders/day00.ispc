export void sums(
    uniform int vals[],
    uniform int starts[],
    uniform int lens[],
    uniform int outs[],
    uniform int count
) {

    /*
    foreach (index = 0 ... count) {
        float v = vin[index];
        if (v < 3.)
            v = v * v;
        else
            v = sqrt(v);
        vout[index] = v;
    }
    */
}
